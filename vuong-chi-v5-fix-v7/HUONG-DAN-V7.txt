VƯƠNG CHÍ V7 - KHẮC PHỤC LỖI MÃ HÓA + FONT + TRANG CÔNG TRÌNH

LỖI V6:
File APPLY-FIX-V6.bat trước đó đọc HTML UTF-8 bằng mã hóa mặc định của Windows PowerShell 5.1 rồi ghi lại UTF-8. Việc đó làm chữ tiếng Việt thành dạng VÆ°Æ NG CHÃ..., XÃ¢Y DÆ°NG... Đây là lỗi của bộ vá V6.

CÁCH DÙNG V7:
1. KHÔNG git push bản chữ đang lỗi.
2. Giải nén TOÀN BỘ gói V7 vào:
   F:\webnhap\vuong-chi-v5
3. Chạy APPLY-FIX-V7.bat.
4. V7 sẽ:
   - sao lưu HTML hiện tại vào _backup-before-v7;
   - dùng git restore để lấy lại HTML từ commit V5 gần nhất;
   - đổi tham chiếu sang styles-v7.css / app-v7.js bằng UTF-8 tường minh;
   - kiểm tra chữ “Vương Chí” và “Xây Dựng” trước khi cho phép tiếp tục.
5. Mở local index.html và du-an.html kiểm tra.
6. Chỉ khi đúng mới chạy:
   git add .
   git commit -m "Fix UTF-8 font and project page v7"
   git push

FONT:
- Ưu tiên Bahnschrift/Bahnschrift Condensed cài trên Windows để giống bản local.
- Nếu trình duyệt online chặn truy cập local font, dùng Barlow Condensed làm web fallback gần nhất.
- Không nhúng file font Microsoft vào project.

TRANG CÔNG TRÌNH:
- app-v7.js nhận diện trang bằng cấu trúc DOM (.filterbar + .project-grid), không phụ thuộc URL phải có .html, nên chạy đúng cả local và Cloudflare Pages.
